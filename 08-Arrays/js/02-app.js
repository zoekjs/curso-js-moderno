// Acceder a los valores de un array

const numeros = [10, 20, 30, 40, 50];

console.log(numeros);

// Una manera fácil de ver arreglos en la consola es con console.table()
// console.table() nos muestra una tabla con el indice y su valor
console.table(numeros);

// Acceder al arreglo
console.log(numeros[2]);
console.log(numeros[0]);

// Si intentamos acceder a un indice que no existe, obtenemos como resultado un undefined